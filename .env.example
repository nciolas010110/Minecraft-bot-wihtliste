# Discord-Bot Token
DISCORD_TOKEN=DeinDiscordBotToken
CLIENT_ID=DeineBotClientID
# Optional: Für schnellen Slash-Command-Refresh im Testserver
GUILD_ID=DeineTestGuildID

# RCON-Verbindung zum Minecraft-Server
RCON_HOST=127.0.0.1
RCON_PORT=25575
RCON_PASSWORD=DeinRconPasswort

# Sicherheits- und Protokollierungseinstellungen
# Discord-Rollen-ID, die neben Administratoren sensible Befehle verwenden darf.
MODERATOR_ROLE_ID=DeineModeratorRollenID
# Kanal-ID für Whitelist-/Ban-Audit-Logs.
AUDIT_LOG_CHANNEL_ID=DeinAuditKanalID

# Optional: Monitoring für Server-Status
# MONITOR_CHANNEL_ID legt den Discord-Kanal fest, in den Warnungen gesendet werden.
# MONITOR_INTERVAL_SECONDS legt das Abfrageintervall in Sekunden fest.
# MONITOR_RAM_THRESHOLD ist der RAM-%-Schwellenwert für eine Warnung.
# MONITOR_TPS_THRESHOLD ist die TPS-Schwelle für eine Warnung.
MONITOR_CHANNEL_ID=1535432171035041864
MONITOR_INTERVAL_SECONDS=60
MONITOR_RAM_THRESHOLD=80
MONITOR_TPS_THRESHOLD=16

# Optional: Discord-Sperren zusätzlich als echten Server-Bann setzen.
# Wenn true, bannt der Bot bei Bereich "Nur Discord" oder "Minecraft und Discord"
# den Benutzer auch vom Discord-Server. Der Bot braucht dafür das Recht "Mitglieder bannen".
# Ohne diese Option wird die Sperre nur gespeichert (kein /whitelist add mehr möglich).
DISCORD_BAN_ENABLED=false

# Server-Fernsteuerung (/server)
# ADMIN_ROLE_ID darf zusätzlich zu echten Administratoren Server-Befehle ausführen.
ADMIN_ROLE_ID=DeineAdminRollenID
# Optionale Sperrliste, kommagetrennt. Diese Befehle lehnt /server konsole immer ab.
# RCON_BLOCKED_COMMANDS=stop,op
# Jail-Plugins nutzen verschiedene Syntax. Platzhalter: {spieler} {jail} {dauer} {grund}.
# Standard passt zu EssentialsX (/jail <Spieler> [Jail] [Dauer]).
# JAIL_COMMAND_TEMPLATE=jail {spieler} {jail} {dauer}
# UNJAIL_COMMAND_TEMPLATE=unjail {spieler}
# JAIL_DEFAULT_NAME=main
